 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/backend/app/services/push_notifications.py b/backend/app/services/push_notifications.py
index 619847dd5ee33b124681d7f67cb99e98194321b5..1dae565ff3092e24bb44e422b64f252a9c9ba77b 100644
--- a/backend/app/services/push_notifications.py
+++ b/backend/app/services/push_notifications.py
@@ -105,50 +105,51 @@ def _active_tokens_for_user(db: Session, user_id: int) -> list[models.PushToken]
         .all()
     )
 
 
 def _deactivate_tokens(db: Session, tokens: Iterable[str]) -> None:
     tokens = [t for t in tokens if t]
     if not tokens:
         return
     rows = db.query(models.PushToken).filter(models.PushToken.expo_push_token.in_(tokens)).all()
     for row in rows:
         row.is_active = False
     if rows:
         logger.info("Deactivating %s Expo push token(s)", len(rows))
         db.commit()
 
 
 def send_push_to_user(
     db: Session,
     *,
     user_id: int,
     title: str,
     body: str,
     data: Optional[dict] = None,
 ) -> None:
     rows = _active_tokens_for_user(db, user_id)
+    logger.warning("ACTIVE PUSH TOKENS user_id=%s count=%s", user_id, len(rows))
     if not rows:
         return
 
     invalid_tokens: list[str] = []
     for row in rows:
         token = row.expo_push_token
         if not _is_valid_expo_token(token):
             invalid_tokens.append(token)
             continue
 
         masked_token = _mask_expo_token(token)
 
         payload = {
             "to": token,
             "sound": "default",
             "title": title,
             "body": body,
             "data": data or {},
             "channelId": "default",
             "priority": "high",
         }
         logger.warning("Sending Expo push to token=%s", masked_token)
         logger.warning(
             "Expo push payload user_id=%s title=%s body=%s data=%s channelId=%s priority=%s sound=%s",
             user_id,
 
EOF
)