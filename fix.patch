 (cd "$(git rev-parse --show-toplevel)" && git apply --3way <<'EOF' 
diff --git a/mobile/src/components/ProviderShareCard.js b/mobile/src/components/ProviderShareCard.js
new file mode 100644
index 0000000000000000000000000000000000000000..49592a0ac4a0524b89b74b93cca17704c594ebb6
--- /dev/null
+++ b/mobile/src/components/ProviderShareCard.js
@@ -0,0 +1,147 @@
+import React from "react";
+import { Image, StyleSheet, Text, View } from "react-native";
+import { theme } from "../theme";
+
+const colors = theme.colors;
+
+export default function ProviderShareCard({
+  avatarUrl,
+  username,
+  professions = [],
+  ratingLabel,
+  brandingSource,
+}) {
+  const safeUsername = String(username || "@bookitgy_provider").trim();
+  const professionText = Array.isArray(professions) && professions.length
+    ? professions.join(" • ")
+    : "BookitGY Provider";
+
+  return (
+    <View style={styles.card}>
+      <View style={styles.brandRow}>
+        <Text style={styles.brandName}>BookitGY</Text>
+        <Text style={styles.brandMeta}>Provider Card</Text>
+      </View>
+
+      <View style={styles.profileRow}>
+        {avatarUrl ? (
+          <Image source={{ uri: avatarUrl }} style={styles.avatar} />
+        ) : (
+          <View style={styles.avatarFallback}>
+            <Text style={styles.avatarInitial}>{safeUsername.charAt(0).toUpperCase()}</Text>
+          </View>
+        )}
+
+        <View style={styles.profileTextWrap}>
+          <Text numberOfLines={2} style={styles.username}>@{safeUsername.replace(/^@/, "")}</Text>
+          <Text numberOfLines={2} style={styles.rating}>{ratingLabel || "★ New on BookitGY"}</Text>
+          <Text style={styles.professions}>{professionText}</Text>
+        </View>
+      </View>
+
+      <View style={styles.footer}>
+        <Text style={styles.cta}>Download BookitGY to schedule your appointment</Text>
+        {brandingSource ? <Image source={brandingSource} style={styles.logo} resizeMode="contain" /> : null}
+      </View>
+    </View>
+  );
+}
+
+const styles = StyleSheet.create({
+  card: {
+    width: "100%",
+    borderRadius: 22,
+    borderWidth: 1,
+    borderColor: "rgba(77,163,255,0.45)",
+    backgroundColor: "#121826",
+    padding: 16,
+    shadowColor: "#4DA3FF",
+    shadowOpacity: 0.2,
+    shadowRadius: 14,
+    shadowOffset: { width: 0, height: 8 },
+    elevation: 6,
+  },
+  brandRow: {
+    flexDirection: "row",
+    justifyContent: "space-between",
+    alignItems: "center",
+    marginBottom: 14,
+  },
+  brandName: {
+    color: colors.text,
+    fontWeight: "700",
+    letterSpacing: 0.5,
+  },
+  brandMeta: {
+    color: colors.textSecondary,
+    fontSize: 12,
+  },
+  profileRow: {
+    flexDirection: "row",
+    alignItems: "flex-start",
+  },
+  avatar: {
+    width: 74,
+    height: 74,
+    borderRadius: 37,
+    borderWidth: 1,
+    borderColor: "rgba(255,255,255,0.25)",
+  },
+  avatarFallback: {
+    width: 74,
+    height: 74,
+    borderRadius: 37,
+    backgroundColor: "#202B45",
+    alignItems: "center",
+    justifyContent: "center",
+    borderWidth: 1,
+    borderColor: "rgba(255,255,255,0.25)",
+  },
+  avatarInitial: {
+    color: colors.text,
+    fontWeight: "700",
+    fontSize: 26,
+  },
+  profileTextWrap: {
+    flex: 1,
+    marginLeft: 12,
+    minWidth: 0,
+  },
+  username: {
+    color: colors.text,
+    fontSize: 21,
+    fontWeight: "700",
+  },
+  rating: {
+    marginTop: 2,
+    color: "#F4D03F",
+    fontWeight: "600",
+  },
+  professions: {
+    marginTop: 8,
+    color: colors.textSecondary,
+    lineHeight: 20,
+  },
+  footer: {
+    marginTop: 16,
+    borderTopWidth: 1,
+    borderTopColor: "rgba(255,255,255,0.12)",
+    paddingTop: 10,
+    flexDirection: "row",
+    alignItems: "center",
+    justifyContent: "space-between",
+    gap: 8,
+  },
+  cta: {
+    color: colors.text,
+    fontSize: 12,
+    lineHeight: 16,
+    fontWeight: "600",
+    flex: 1,
+  },
+  logo: {
+    width: 56,
+    height: 24,
+    opacity: 0.9,
+  },
+});
 
EOF
)